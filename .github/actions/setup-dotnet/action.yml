name: Setup .NET
description: Sets up .NET SDK for building projects

inputs:
  dotnet-version:
    description: .NET SDK version to install
    required: true

runs:
  using: composite
  steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ inputs.dotnet-version }}
